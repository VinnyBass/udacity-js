//if ternario
var eatsPlants = true;
var eatsAnimals = true;

var category = (eatsPlants && eatsAnimals) ? "omnivore" : eatsAnimals ? "carnivore" : eatsPlants ? "herbivore" : undefined;

//Separar os problemas em passos logicos
//Ver quais as entradas de dados e quais a saidas
//COmo voce resolveria isso e converter em passos logicos